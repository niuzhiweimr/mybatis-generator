package com.mg.constant;

import java.util.Locale;
import java.util.ResourceBundle;

public class Constant {

	// 配置文件路径
	public static final String MYBATIS_XML_FILE_PATH = "mybatis-generator.xml";

	// pojo类后缀
	public static final String CLASS_SUFFIX = ResourceBundle.getBundle("common-config", Locale.CHINA).getString("pojo-suffix");

	// pojo类名称修改 通用前缀的长度
	public static final int CLASS_UP_PREFIX = Integer.valueOf(ResourceBundle.getBundle("common-config", Locale.CHINA).getString(
			"pojo-upPrefix"));

	// dao接口后缀
	public static final String DAO_SUFFIX = ResourceBundle.getBundle("common-config", Locale.CHINA).getString("dao-suffix");

	// dao接口名称修改 通用前缀的长度
	public static final int DAO_UP_PREFIX = Integer.valueOf(ResourceBundle.getBundle("common-config", Locale.CHINA).getString(
			"dao-upPrefix"));

	// mapper映射文件后缀
	public static final String MAPPER_SUFFIX = ResourceBundle.getBundle("common-config", Locale.CHINA).getString("mapper-suffix");

	// mapper映射文件名称修改 通用前缀的长度
	public static final int MAPPER_UP_PREFIX = Integer.valueOf(ResourceBundle.getBundle("common-config", Locale.CHINA).getString(
			"mapper-upPrefix"));

	// 接口前缀
	public static final String INTERFACE_PREFIX = ResourceBundle.getBundle("common-config", Locale.CHINA).getString(
			"interface-prefix");

	// 通用sql前缀
	public static final String COMMON_SQL_PREFIX = ResourceBundle.getBundle("common-config", Locale.CHINA).getString(
			"common-sql-prefix");

	// 通用插入前缀
	public static final String COMMON_INSTER_PREFIX = ResourceBundle.getBundle("common-config", Locale.CHINA).getString(
			"common-inster-prefix");

	// 通用批量插入前缀
	public static final String COMMON_BACTH_INSTER_PREFIX = ResourceBundle.getBundle("common-config", Locale.CHINA).getString(
			"common-bacth-inster-prefix");

	// 通用更新前缀
	public static final String COMMON_UPDATE_PREFIX = ResourceBundle.getBundle("common-config", Locale.CHINA).getString(
			"common-update-prefix");

	// 通用批量更新前缀
	public static final String COMMON_BACTH_UPDATE_PREFIX = ResourceBundle.getBundle("common-config", Locale.CHINA).getString(
			"common-bacth-update-prefix");

	// 通用删除前缀
	public static final String COMMON_DELETE_PREFIX = ResourceBundle.getBundle("common-config", Locale.CHINA).getString(
			"common-delete-prefix");

	// 通用批量删除前缀
	public static final String COMMON_BACTH_DELETE_PREFIX = ResourceBundle.getBundle("common-config", Locale.CHINA).getString(
			"common-bacth-delete-prefix");

	// 通用全部删除前缀
	public static final String COMMON_DELETE_ALL_PREFIX = ResourceBundle.getBundle("common-config", Locale.CHINA).getString(
			"common-delete-all-prefix");

	// 通用查询列表前缀
	public static final String COMMON_SELECT_LIST_PREFIX = ResourceBundle.getBundle("common-config", Locale.CHINA).getString(
			"common-select-list-prefix");

	//控制 插入是否要加入主键  true添加   false不添加
	public static final Boolean  INSTER_IS_PK=Boolean.valueOf(ResourceBundle.getBundle("common-config", Locale.CHINA).getString(
			"inster-is-pk"));
}
